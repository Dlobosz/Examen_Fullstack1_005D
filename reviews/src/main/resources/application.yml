server:
  port: 9003

spring:
  application:
    name: reviews
  datasource:
    url: jdbc:mysql://localhost:3313/reviews?useSSL=false&serverTimezone=UTC
    username: examen
    password: examen123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true  
  flyway:
    enabled: true
    baseline-on-migrate: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/      
